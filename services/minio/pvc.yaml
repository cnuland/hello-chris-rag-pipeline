---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio
  nasmespace: minio
  labels:
    app: minio
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi