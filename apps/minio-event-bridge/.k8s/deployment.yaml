apiVersion: v1
kind: Service
metadata:
  name: minio-event-bridge
  namespace: rag-pipeline-workshop
  labels:
    app: minio-event-bridge
    app.kubernetes.io/part-of: rag-pipeline
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: minio-event-bridge
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: minio-event-bridge
  namespace: rag-pipeline-workshop
  labels:
    app: minio-event-bridge
    app.kubernetes.io/part-of: rag-pipeline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: minio-event-bridge
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "true"
      labels:
        app: minio-event-bridge
        app.kubernetes.io/part-of: rag-pipeline
    spec:
      containers:
      - name: event-bridge
        image: quay.io/cnuland/hello-chris-rag-pipeline-converter:latest  # This will be replaced by the actual image name in the pipeline
        imagePullPolicy: Always
        env:
        - name: BROKER_URL
          value: "http://kafka-broker-ingress.knative-eventing.svc.cluster.local/rag-pipeline-workshop/kafka-broker"
        - name: LOG_LEVEL
          value: "DEBUG"
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20

